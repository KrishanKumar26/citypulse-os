[pytest]
# `common` and `generator` are imported as top-level packages by the Spark job,
# the local runner and these tests alike, so the repo root for them is this
# directory rather than an installed distribution.
pythonpath = .
testpaths = tests
addopts = -q
